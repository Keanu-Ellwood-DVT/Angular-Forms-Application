import { IdentificationNumberDirective } from './identification-number.directive';

describe('IdentificationNumberDirective', () => {
  it('should create an instance', () => {
    const directive = new IdentificationNumberDirective();
    expect(directive).toBeTruthy();
  });
});
