import { IdNumberValidationFactory } from './id-number-validation-factory';

describe('IdNumberValidationFactory', () => {
  it('should create an instance', () => {
    expect(new IdNumberValidationFactory()).toBeTruthy();
  });
});
